llm:
  provider: "google"
  model_name: "gemini-2.0-flash"
  temperature: 0.1
  max_retries: 3

paths:
  data_dir: "data/legal_case/"
  results_dir: "data/legal_case/results/"
  prompts_dir: "prompts/"
